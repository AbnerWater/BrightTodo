# BrightToDo 中期改进任务验收记录（2026-06-14）

## 一、验收范围

本记录对应中期评审反馈后的稳定化冲刺改进，覆盖团队改进计划表中的 A-01 至 A-05。验收目标是确认现场演示反馈已经落实到可验证的产品行为、接口契约、测试用例和过程留痕中。

## 二、逐项验收记录

| 编号 | 反馈问题 | 本轮完成情况 | 验收证据 | 关闭状态 |
| --- | --- | --- | --- | --- |
| A-01 | 日程冲突提示不够清晰 | 后端 `ScheduleSuggestRequest` 新增 `blocked_slots`，与原有 `schedule_constraints` 一起参与候选时段过滤；冲突说明更新为“无课程/已有待办冲突”；前端规划面板展示未安排原因、推荐时段和备选时段。 | `tests/test_schedule_suggest_service.py` 覆盖已有待办占用避让、课程约束避让、无可用时段错误和备选时段返回；附件/文本规划界面展示 `scheduleReason`。 | 已完成，已验收 |
| A-02 | Todo Agent 自动推荐空闲时间能力需增强 | 自然语言规划和附件规划均会把当前 Todo 列表中的固定时间任务转为 `blocked_slots`；带 `rrule` 的周重复待办在 7 天规划窗口内展开；后端将 `ScheduleSuggestService` 返回的建议开始/结束时间写入待确认规划项，并返回备选时段。 | `tests/test_agent_attachment_plan_service.py` 覆盖附件规划和文本规划在已有占用时段后的自动顺延；`tests/test_agent_router.py` 覆盖 `/schedule-suggest`、`/text-plan`、`/attachment-plan` 新字段契约。 | 已完成，已验收 |
| A-03 | LLM API 调用稳定性不足 | 已合入 `fix(backend): add responses api llm path`，统一非流式文本优先走 OpenAI 兼容 `/v1/responses`，仅在识别为端点不支持时回退 Chat Completions；配置测试接口返回 `api_mode`。 | `tests/test_llm_client_responses.py`、`tests/test_llm_config_responses.py` 已通过；`tests/test_agent_import_service.py` 验证文件导入文本通路走统一 `chat(...)` 入口。 | 已完成，已验收 |
| A-04 | 附件识别规划过程体验需优化 | 前端两个附件规划入口统一文件状态、识别摘要、规划失败提示和重试入口；后端文件结果中的 `raw_text_preview` 已进入前端类型；待确认规划项继续支持标题、优先级、截止时间、时长、建议开始/结束、说明编辑，并保留 separate/nested 创建方式。 | `pnpm type-check` 和 `pnpm check` 已通过；附件规划失败后保留当前文件与 prompt，用户可直接点击“重试规划”。 | 已完成，已验收 |
| A-05 | Sprint 评审反馈与验收留痕需完善 | 新增本验收记录，并更新 Word 版团队改进计划表状态与验收证据；代码、测试和文档随同一任务分支提交，便于组长统一归档。 | `docs/06_过程记录/中期改进任务验收记录_2026-06-14.md` 与 `docs/06_过程记录/团队改进计划表_中期评审反馈.docx` 已纳入交付。 | 已完成，已验收 |

## 三、验证命令

- `python -m pytest tests/test_agent_import_service.py tests/test_agent_attachment_plan_service.py tests/test_agent_router.py tests/test_schedule_suggest_service.py tests/test_agent_parse_service.py tests/test_llm_client_responses.py tests/test_llm_config_responses.py -q`
- `ruff check lifetrace tests`
- `pnpm type-check`
- `pnpm check`

## 四、交付确认

核心 ToDo Agent 演示链路已完成阶段性稳定化改进；中期评审现场反馈的 4 项问题已落实到功能、接口、测试和过程记录中。本轮交付状态更新为“已完成，已验收”，后续继续在实际演示环境中收集边界场景反馈。
